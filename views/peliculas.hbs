{{> navegacion}}

<h1>Películas</h1>


<ul class="element">
        {{#each peliculas}}
        <li>
            <div class="caja">
                <img src="{{imagen.secure_url}}"
                    alt="{{nombre}}">
                    <a href="/peliculas//{{_id}}" class="btn btn-primary">Ver mas</a>
                <figcaption> {{nombre}}</figcaption>

            </div>
        </li>
        {{/each}}
    </ul>


{{> footer}}